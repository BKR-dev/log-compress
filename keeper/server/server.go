package server

func main() {

}

func mux() {
	// r := mux.NewRouter()
	// r.HandleFunc("/", HomeHandler)
	// r.HandleFunc("/logs", logsHandler)
	// r.HandleFunc("/times", timesHandler)
	// http.Handle("/", r)
}
